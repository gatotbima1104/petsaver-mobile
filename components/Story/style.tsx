import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  borderImage: {
    borderColor: 'red',
    padding: 3,
    borderWidth: 1,
    borderRadius: 100,
  },
});

export default style;
